<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingTop="15dp"
    android:paddingBottom="15dp">

    <ImageView
        android:id="@+id/imgIconDCItem"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:src="@drawable/ic_99"/>

    <TextView
        android:id="@+id/txtTitleDCItem"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/imgIconDCItem"
        app:layout_constraintBottom_toBottomOf="@id/imgIconDCItem"
        app:layout_constraintStart_toEndOf="@id/imgIconDCItem"
        app:layout_constraintEnd_toStartOf="@id/image"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="10dp"
        />

    <ImageView
        android:id="@+id/image"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_75"/>

</androidx.constraintlayout.widget.ConstraintLayout>